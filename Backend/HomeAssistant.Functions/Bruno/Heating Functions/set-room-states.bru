meta {
  name: set-room-states
  type: http
  seq: 2
}

post {
  url: http://localhost:7159/api/room-states?houseId={{houseId}}
  body: none
  auth: inherit
}

params:query {
  houseId: {{houseId}}
}

settings {
  encodeUrl: true
  timeout: 0
}
