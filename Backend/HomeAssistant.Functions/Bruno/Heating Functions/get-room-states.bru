meta {
  name: get-room-states
  type: http
  seq: 3
}

get {
  url: {{function-endpoint}}/api/room-states?houseId={{houseId}}
  body: none
  auth: inherit
}

params:query {
  houseId: {{houseId}}
}

settings {
  encodeUrl: true
  timeout: 0
}
