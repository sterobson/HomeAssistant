meta {
  name: get-schedules
  type: http
  seq: 5
}

get {
  url: {{function-endpoint}}/api/schedules?houseId={{houseId}}
  body: none
  auth: inherit
}

params:query {
  houseId: {{houseId}}
}

settings {
  encodeUrl: true
  timeout: 0
}
